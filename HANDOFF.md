# 引き継ぎメモ（2026-08-20更新）

競馬指数プロジェクト。分析→優先指数の設計→webapp実装まで一通り完了。
このメモは Claude セッションから Codex／次のClaudeセッションへ引き継ぐためのもの。

## 2026-08-20の作業（PC→端末間共有をGit不要のOneDrive方式へ全面移行）

- **発端**：8/16分のデータが「最新データを取得」で表示されない、という報告。原因は`publish_data.py`が8/15までしか手動実行されておらず、`webapp/data/`（GitHub Pages公開データ）に8/16が反映されていなかったこと。「最新データを取得」は公開済みデータを取ってくるだけで、新しい日付を自動追加する仕組みではないため、この手動publishステップを忘れると新着分が他端末に届かないという構造的な問題があった。
- **ユーザー判断で、Git経由の公開データ方式を完全に廃止しOneDrive直接共有方式に一本化**。既に前日(8/19)に実装していた「OneDriveへ書き出す」（`writeShareFile()`、File System Access APIでOneDrive等の同期フォルダに直接JSON書き出し、PC限定）「OneDriveから読み込む」（`createShareReadPicker()`、`<input type=file>`なのでiPhoneでも同じボタンで使える）の2ボタンだけを正式な共有経路とした。
- **削除したもの**：`publish_data.py`、`webapp/data/`以下の公開JSON全て（`index.json`と日付ごとのJSON）、`app.js`の`fetchPublished()`・`syncButton`・`state.publishedDates`・起動時の公開データチェック処理。`webapp/README.md`の該当セクションも書き換え済み。
- **効果**：指数データが公開リポジトリ（`yotani27-glitch/keiba-sisu`）を一切経由しなくなった。以前は「本当は公開したくないデータを、cross-device共有のためだけに公開リポジトリに置く」というプライバシー上のトレードオフを許容していたが、これが解消された。
- **残る制約**：iPhone(Safari)はFile System Access API非対応のため、読み込み側は「OneDriveから読み込む」ボタンでファイルを1回選ぶ操作が必須（完全自動化は不可）。書き出し側もChromium系デスクトップ限定（PC側はChrome/Edgeを使用中なので問題なし）。

## 2026-08-19の作業（複勝率相関の指数別分析＋区分別複勝優先指数の実装）

- **各指数の複勝率相関を実測**（2022-2026年・15,903レース・217,147頭）。序列はＦ指数0.330＞Ｓ指数0.305＞TUA指数0.278＞arms指数２0.213＞厩舎Finish-Up0.199＞LVL2 0.186＞＞血統系・バランス・坂路調教(0.01〜0.09)。勝率ベースの序列(TUA<arms2)と入れ替わりがある点に注意。
- **場所別・距離帯別のＦ/Ｓ指数相関を検証**。場所別の差は前後半再現性0.822だが効果量が小さく(相関差0.01〜0.04)、場所ごとに式を分けるほどの根拠なしと判断。**距離帯×芝ダは再現性0.955と遥かに高く**、①芝は距離が延びるほどＦ指数優位が拡大(短距離ほぼ互角→長距離+0.075〜0.078) ②**ダート1801m以上だけはＳ指数がＦ指数を上回る唯一の区分**、という明確なパターンを確認。場所×距離帯まで細分化すると再現性0.328まで落ちるため、そこまでは踏み込まない方針に。
- **Ｆ指数の生値に閾値効果を確認**：1位馬に限るとＦ指数値71以上で複勝率63.7%/63.9%(前後半)、61未満は48-49%。頭数で層別しても崩れない。ただし全馬(順位問わず)で見るとＦ・Ｓ・arms2いずれも生値と複勝率はなめらかな単調増加で、「1位馬限定の閾値」と「全馬の連続的な関係」は別物と理解すること。
- **Ｆ指数の1位-2位差(gap)は値の水準とセットでないと意味がない**：値71以上・61-70ではgap大が複勝率を+1〜5pt押し上げるが、**値61未満ではgap大がむしろ-9〜11pt下げる**(フィールド全体が弱いレースを拾っている可能性、前後半とも再現)。
- **★複勝優先指数にダート1801m以上の区分別重みを実装**（`webapp/app.js`）。詳細は下記「区分別複勝優先指数（2026-08-19実装）」を参照。
- **`競争馬名/name<日付>.csv`の実際の形式を確認・本ドキュメントの誤記を修正**。旧記述「18桁ID,馬名」の2列形式は誤りで、実際は32列（場所+R, クラス名, **芝/ダ**, **距離**, ..., 馬番, ..., 馬名, ..., 性別, 年齢, ..., 騎手, 斤量, 厩舎, 調教師）のTARGET出馬表CSV。`app.js`は既にこの新形式を読んでいた(馬番=6列目, 馬名=10列目)が、芝ダ(3列目)・距離(4列目)は未使用だった。今回、区分別重み判定用に読み込むよう拡張した。

## 2026-08-18の作業（フォルダ整理＋8/15・8/16の検証）

- **フォルダ整理**: Desktop側・OneDriveルート直下にあった2つの`NG+WE`（Codexサンドボックスの重複コピー、計約440MB）を、内容確認（バイト比較で既存マスターCSVと完全一致）の上、ユーザーが削除済み。参考資料は`指数\旧デスクトップ_NG+WE\`に一本化。
- **マスターCSVの運用方針を変更**: `指数_2026年.csv`（単一年ファイル）は廃止し、`指数フォルダ/2026/YYYYMMDD.csv`の日別ファイルに分割済み（67日分、旧ファイルは`指数フォルダ/_archive/`に退避）。2022〜2025年は従来通り年別1ファイルのまま。
- **新設フォルダ `レース結果/`**: TARGET/週次ZIPに指数がまだ揃っていない直近日について、NotebookLM経由でJRA公式結果ページから取得した結果データを日別に保存する置き場。指数（Ｆ指数等）は含まない。
  - `20260815.csv`/`20260816.csv`: 着順・オッズ・馬体重・ラップ・コーナー通過等（うまのーとのラップとJRA結果をNotebookLM経由で結合）
  - `払戻_20260815.csv`/`払戻_20260816.csv`: 単勝・複勝・枠連・ワイド・馬連・馬単・3連複・3連単の全券種
  - **一度マスターCSVに直接追記したが（977行、指数列は空欄）、ユーザー指示で元に戻した**。今後もレース後の結果データはこの`レース結果/`に日別保存し、TARGET等で指数付きの正式データが揃ってから初めてマスターCSV（`指数フォルダ/2026/`）に反映する方針。
- **8/15・8/16の優先指数バックテスト実施**（週次ZIPの生指数から自前で優先指数を再計算し、レース結果と突き合わせ）:
  - 優先1位: n=72 勝率23.6% 複勝52.8% 単勝回収率72.8% 複勝回収率85.3%（5年実測: 勝率24.4%/複勝54.8%/単勝78-82%とほぼ整合）
  - 優先2位: 単勝53.5%／優先3位: 単勝39.0%
  - クラス別ではサンプルが厚い「３歳未勝利」(n=22)「３歳以上1勝クラス」(n=17)の優先1位が特に好調（複勝85-92%）
  - 指数値帯（0.90/0.92/0.94以上）×クラスのクロス集計はn=1〜6まで細分化されすぎて解釈不能と判断、参考にしない
  - **この2日間だけの結果なので、5年分の実測傾向を覆すものではない。「今後の積み重ね」という位置づけ**（ユーザー発言）
- **NotebookLMのクォータ制限に注意**: `notebook_query`は1日に数十回で`RESOURCE_EXHAUSTED`(error code 8)に達しやすい。`source_get_content`でソース本文を直接読む方式の方が安定。`nlm login profile list`で`company`/`default`の2アカウントがあり、切替は`nlm login switch <profile>`。

## リポジトリ

- `yotani27-glitch/keiba-sisu`（**公開**リポジトリ、GitHub Pages配信）
- 公開URL: https://yotani27-glitch.github.io/keiba-sisu/
- push すると数分でPagesに自動デプロイされる（`.github/workflows/pages.yml`）
- **注意**: 公開リポジトリなので、指数の生データや購読データそのものは絶対に置かない。webappは「ブラウザがローカルのZIP/CSVを読む」方式が基本で、データはリポジトリに含めていない（2026-08-20より、端末間共有もOneDrive経由に一本化したため`webapp/data/`のような公開データ置き場も廃止済み）

## ディレクトリ構成

```
指数/                          ← このリポジトリのルート
├── priority_index.py          ← マスターCSVに優先指数列を追加するツール（ローカル専用、非コミット）
├── weekly_priority_report.py  ← 未確定レースの優先指数ランキングをテキスト出力（ローカル専用）
├── HANDOFF.md                 ← このファイル
└── webapp/
    ├── index.html
    ├── app.js                 ← メインロジック（すべてここに集約、680行→1000行超）
    ├── styles.css
    └── README.md               ← 機能の使い方・実測値の説明（詳しいのでまず読む）

指数フォルダ/                  ← 週次ZIPの置き場（TARGET frontier形式、gitignore対象）
競争馬名/                      ← TARGET出馬表CSV（name<日付8桁>.csv、gitignore対象。
                                   32列、場所+R/クラス名/芝ダ/距離/馬番/馬名/性別/年齢/
                                   騎手/斤量/厩舎/調教師を含む。詳細は下記）
厩舎Finish-Up/                 ← 厩舎指数CSV（<日付8桁>.csv、gitignore対象）
```

マスターCSV本体（2022〜2026年の5ファイル）は **2026-08-16に`指数フォルダ/`へ移動済み**:
`指数フォルダ\指数_2022年.csv`〜`指数_2026年.csv`
（UTF-8 BOM付き、50列。以前は`C:\Users\Y_OOTANI\Desktop\NG+WE\`にあったが、
Desktop側は別のgitリポジトリ（Codexのサンドボックス、所有者が別Windowsアカウント）に
なっていたため、混在を避けてこのリポジトリ内に一本化した。Desktop側の同名ファイルは
削除済み。`指数フォルダ`は`.gitignore`で`*.csv`が除外されているため非公開のまま）

## 優先指数（分析で確定した仕様）

2022〜2026年・15,414レース・211,848頭（芝・ダートのみ、障害除く）で検証。

```
優先スコア = (4×Ｆ指数 + 3×Ｓ指数 + 2×arms指数２ + 2×LVL2 + 2×厩舎Finish-Up) / 13
※各項はレース内で正規化した順位 = (出走頭数 − 順位) / (出走頭数 − 1)
```

- 重みは2022-2024年で条件付きロジット（レース内softmax）により推定、2025-2026年で検証。年ごとに推定しても寄与率はほぼ同じ（Ｆ40-45%/Ｓ34-37%/arms15-19%）
- **血統系4指数・バランス指数・坂路調教指数は使わない**（1位馬勝率が5年間ずっとランダム基準7.3%と区別できない。混ぜると精度が落ちる）
- **TUA指数も不使用**（寄与2-6%と小さく年による振れが大きい。webapp表示からも削除済み）
- レース内で全馬に揃っている指数だけを使い、重みを再正規化する仕組み。指数が欠けている年（LVL2は2022年に無し、厩舎Finish-Upは2025年8月以降のみ）でも3〜5指数で自動的に算出できる

### アウトオブサンプル検証結果（2025-2026年）

- 優先1位馬: 勝率24.4% / 複勝率54.8%
- Ｆ指数単独: 勝率24.0% / 複勝率54.4%
- 上積みは1位的中では+0.4ptと小さいが、**絞り込み力**が本物（上位3頭に勝ち馬を含める率+3.4pt、上位5頭+5.4pt）→ 本命1頭を決める道具ではなく相手を絞る道具

### 回収率について（重要）

**単勝回収率が継続的に80%（払戻率）を超える条件は見つかっていない。** 優先順位・スコア水準・1-2位差・レース形（後述）・競馬場・距離・頭数、どの切り口で切っても73-88%のレンジに収まり、前半後半で回収率の相関は低い（勝率の相関0.877に対し回収率は0.298）。**このツールは的中率向上の道具であり、回収率を上げる道具ではない**、という結論で一貫している。複勝・馬連等の配当データはCSVに無いため未検証。

## webappに実装済みの機能

`webapp/app.js` に全ロジックがある。主要な定数・関数名:

1. **優先指数の計算** — `PRIORITY_WEIGHTS`, `computePriorityForRace()`
2. **堅さ判定**（優先上位2頭 vs 人気上位2頭の一致数） — `FIRMNESS`（予想人気GYN基準）, `FIRMNESS_ODDS`（確定オッズ基準）, `buildRaceSummaries()`
   - GYN基準: 一致2=30.1%/1=24.6%/0=17.1%（週次ZIPに入っているのでレース前でも判定可）
   - 確定オッズ基準: 一致2=31.9%/1=24.8%/0=11.0%（当日人気を手入力すると切替）
3. **上位3頭の並び方**（独走・2強・団子） — `SHAPE_PATTERNS`, `raceShape()`, `GAP12_MEDIAN=0.056`, `GAP23_MEDIAN=0.043`
   - 独走: 勝率30.7%/複勝61.4%、2強: 23.3%/53.9%、団子: 19.5%/48.9%
   - **直近の未実装アイデア**: 形×堅さのクロス集計で最良「独走×一致2」35.8%・最悪「団子×一致0」10.2%まで分離することを確認済み（詳細は下記「積み残しタスク」）
4. **当日人気の入力** — `.popular-input`、`localStorage`キー`keiba-popular-v1`
5. **内訳指数の表示**（Ｆ・Ｓ・arms2・厩舎、値と順位、馬番順・全頭表示） — `BREAKDOWN`配列（表示順は厩>S>F>ar、ユーザー指定）
6. **読み込みデータの永続化** — IndexedDB（`saveCache()`/`restoreCache()`、生の値のみ保存し順位・優先スコアは開くたび再計算）
7. **PC→iPhoneのデータ共有（OneDrive経由、2026-08-20全面移行）** — `writeShareFile()`でOneDrive同期フォルダにJSON書き出し（PC限定）→`createShareReadPicker()`で他端末がファイル選択して読み込み。Git不要、指数データは公開リポジトリを経由しない
8. **当日人気の共有リンク** — `encodePopular()`/`decodePopular()`、URLの`#pop=...`に載せる（サーバー不要）
9. **iPhone向けファイル選択** — フォルダ選択非対応のiOS用に`createFilePicker()`でZIP単体選択に対応
10. **区分別複勝優先指数（2026-08-19実装）** — ダート1801m以上だけＳ指数優位の重みに切り替える。詳細は次項。

### 区分別複勝優先指数（2026-08-19実装）

`名<日付>.csv`（TARGET出馬表CSV）に芝ダ・距離が入っていることが判明したため、`processCsvEntry()`の新形式パース部分（`app.js` 291行目付近）で`parts[2]`(芝/ダ)・`parts[3]`(距離)を`rec.surface`/`rec.distance`として読み込むよう拡張した。

`computePriorityForRace()`は、レース内のいずれかの馬に`surface`/`distance`が設定されていれば（＝`name<日付>.csv`を読み込み済みなら）、`isDirtLongDistance()`でダート1801m以上と判定し、複勝優先指数の計算に`PLACE_PRIORITY_WEIGHTS_DIRT_LONG`（Ｆ6:Ｓ7:LVL2 3:厩舎3、arms指数２は使わない）を使う。`name<日付>.csv`を読み込んでいない場合は従来通り`PLACE_PRIORITY_WEIGHTS`（Ｆ8:Ｓ5:arms2 3:LVL2 3:厩舎3）のまま。

該当レースはカードの`.rmeta`欄に「ダ長」バッジ（緑、`.dirt-long-badge`）が付く。根拠は2022-2024年学習→2025-2026年検証で1位馬の複勝率53.1%→58.1%(+5.0pt, n=358)。

**注意**：この機能を使うには`name<日付>.csv`を毎回読み込む必要がある（従来は馬名表示のためだけだったが、今後は区分判定にも必須になった）。読み込んでいないレースは自動的に従来の重みにフォールバックするので壊れることはないが、精度向上の恩恵を受けるには読み込みが必要。

## 週次の運用フロー（2026-08-20更新、Git不要）

1. PCで週次ZIP・馬名CSV・厩舎Finish-Up CSV・発走時刻Excelをダウンロードしてそれぞれの置き場に保存（従来通り）
2. webappの各ボタンで読み込む（指数ZIP／厩舎・馬名CSV／発走時刻Excel）
3. PCで「**OneDriveへ書き出す**」を押す → OneDrive同期フォルダにJSONが書き出される（初回のみ保存先を選ぶ、以降は上書き）
4. OneDriveの同期を待ってから、他端末で「**OneDriveから読み込む**」→ 同じファイルを選ぶ
5. 当日、人気を確認して各レースカードに入力 → 堅さ判定が確定オッズ基準に切替
6. 当日人気だけを他端末に送りたい場合は引き続き「共有リンクを作る」が使える（こちらはURL埋め込み方式でOneDriveとは別経路）

## 積み残しタスク（次のセッションでやること）

直前の会話で「形×堅さ」のクロス集計を分析済み（相関係数: gap12×一致数=0.052、gap23×一致数=0.185、ほぼ独立）。組み合わせると単独より広いレンジに分離することを確認:

```
独走×一致2  n=1280  勝率35.8%  複勝67.1%  （最良）
団子×一致0  n=581   勝率10.2%  複勝31.5%  （最悪）
```

ユーザーに「webappに形×堅さのクロス表示を追加するか」を提案した直後にこの引き継ぎ依頼が来た。**ユーザーの返答はまだ受け取っていない**ので、次のセッションはここから再開すること。実装するなら:
- カードのバッジ表示（形バッジ＋堅さバッジは既にある）に加えて、両方が「最良」「最悪」の組み合わせのときだけ強調する視覚表現を検討
- 分析スクリプトは `C:\Users\Y_OOTANI\AppData\Local\Temp\claude\...\scratchpad\g3_shape_vs_firmness.py` に一時保存されているが、セッション固有の一時ディレクトリなので再利用不可。同じロジックを再実装する必要がある

## 検証の作法（このセッションで徹底していたこと）

- 変更のたびにローカルサーバー（`python -m http.server 8765 --directory webapp`、`.claude/launch.json`に設定済み）で実データ（`指数フォルダ/2tua20260809.zip`等）を読み込ませ、`javascript_tool`でDOM/state を直接検査して検証してからpush
- モバイル幅(375px)での横スクロール・はみ出しも毎回確認
- push後はGitHub Pagesのデプロイ完了を待ってから公開URLでも同じ検証を再実行
- コミットメッセージは日本語、`Co-Authored-By: Claude ... <noreply@anthropic.com>`を付与
- ブラウザキャッシュで新しいapp.jsが反映されないことが度々あったので、検証時は`cache:'reload'`のfetchや`?cb=...`のキャッシュバスターを使うこと

## ユーザーについて

- 競馬の指数分析・馬券検討を主目的にしている
- 技術的な実装の妥当性より「実測値の裏付けがあるか」を重視する（毎回5年分データでの検証を求める）
- UIの細部（文字の中央揃え、表示順など）にも具体的な指示を出す
- 「回収率が上がる条件」を繰り返し探しているが、これまで一貫して見つかっていない旨を正直に伝え続けている
